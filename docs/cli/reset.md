---
summary: "CLI reference for `Xcriminal reset` (reset local state/config)"
read_when:
  - You want to wipe local state while keeping the CLI installed
  - You want a dry-run of what would be removed
---

# `Xcriminal reset`

Reset local config/state (keeps the CLI installed).

```bash
Xcriminal reset
Xcriminal reset --dry-run
Xcriminal reset --scope config+creds+sessions --yes --non-interactive
```

