---
summary: "CLI reference for `Xcriminal uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
---

# `Xcriminal uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
Xcriminal uninstall
Xcriminal uninstall --all --yes
Xcriminal uninstall --dry-run
```

